/**
 * Selection feature — observes selectionAtom.
 *
 * v1: no rendered UI. The click hookup lives in interaction/useClick,
 * which writes selectionAtom; this feature is the placeholder consumer.
 */
export const SelectionFeature = () => null;
