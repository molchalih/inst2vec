# Data bundles are NOT committed. The frontend is served from the atlas API
# (serving DB); we no longer ship a static JSON bundle. Locally generated
# fixtures (manifest.json, runs/) stay ignored here.
public/data/*
