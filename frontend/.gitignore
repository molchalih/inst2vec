# Local/scratch data bundles — not committed (mock/, run_v1/, … ; see export pipeline)
public/data/*
# ...except the real exported atlas bundle: manifest + runs
!public/data/manifest.json
!public/data/runs/
