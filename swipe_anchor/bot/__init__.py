"""Telegram bot: the access gate + Mini App launcher for swipe-anchor.

A thin, swappable wrapper over the existing access-code identity. Run with::

    uv run --group bot python -m swipe_anchor.bot
"""
